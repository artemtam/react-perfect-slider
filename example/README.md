# react-perfect-slider demo (Vite + React)

Demo app for `react-perfect-slider` using Vite + React + TypeScript. It imports the component directly from the workspace `src` for easy development.

## Run locally

```bash
cd examples/vite-react
bun install
bun run dev
```

Then open the printed Local URL.
